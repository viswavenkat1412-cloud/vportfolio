export declare const BREAK_LENGTH = 80;
export declare const MAX_ARRAY_LENGTH = 100;
export declare const enum Kind {
    Object = 0,
    ArrayItem = 1,
    ArrayExtras = 2
}
export interface Ctx {
    depth: number;
    indentationLvl: number;
    currentDepth: number;
    seen: unknown[];
    circular: Map<unknown, number> | undefined;
}
/** `reduceToSingleString()` of `lib/internal/util/inspect.js`, `compact: 3`. */
export declare const reduceToSingleString: (ctx: Ctx, output: string[], base: string, braces: string[], kind: Kind, recurseTimes: number, value: unknown) => string;
