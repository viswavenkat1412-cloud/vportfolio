import { Buffer } from '@jsonjoy.com/fs-node-builtins/lib/internal/buffer';
export interface IReadOptions {
    offset?: number | null;
    length?: number | null;
    position?: number | bigint | null;
}
export interface IReadParams extends IReadOptions {
    buffer?: Buffer | ArrayBufferView | DataView;
}
export interface IWriteOptions {
    offset?: number | null;
    length?: number | null;
    position?: number | null;
}
export type ReadCallback = (err?: Error | null, bytesRead?: number, buffer?: Buffer | ArrayBufferView | DataView) => void;
export interface ReadArgs {
    buffer: ArrayBufferView;
    offset: number;
    length: number;
    /** `null` reads from the file description's current position. */
    position: number | null;
}
export interface ReadCallbackArgs extends ReadArgs {
    callback: (...args: any[]) => void;
}
/**
 * `readSync(fd, buffer, offsetOrOptions, length, position)`.
 *
 * @param argc How many arguments the caller passed, `fd` included: three or fewer make the third an
 *   options object even when it is a number.
 */
export declare const getReadSyncArgs: (argc: number, buffer: unknown, a?: unknown, b?: unknown, c?: unknown) => ReadArgs;
/**
 * `read(fd, buffer, offsetOrOptions, length, position, callback)`.
 *
 * @param argc How many arguments the caller passed, `fd` included: four or fewer select one of the
 *   options, params or bare-callback forms.
 */
export declare const getReadArgs: (argc: number, buffer: unknown, a?: unknown, b?: unknown, c?: unknown, d?: unknown) => ReadCallbackArgs;
/** `filehandle.read(buffer | params, offset | options, length, position)`. */
export declare const getHandleReadArgs: (bufferOrParams: unknown, a?: unknown, b?: unknown, c?: unknown) => ReadArgs;
/** `filehandle.write(buffer | string, offset | options | position, length | encoding, position)`. */
export declare const getHandleWriteArgs: (data: unknown, a?: unknown, b?: unknown, c?: unknown) => WriteArgs | null;
/** `readv`/`writev` take the position only when it is a number, and libuv seeks only to a safe offset. */
export declare const getVectorArgs: (buffers: unknown, position: unknown) => number | null;
export declare const getVectorCallbackArgs: (buffers: unknown, a: unknown, b: unknown) => [position: number | null, callback: (...args: any) => void];
export declare const getWriteArgs: (fd: number, a?: unknown, b?: unknown, c?: unknown, d?: unknown, e?: unknown) => [fd: number, dataAsStr: boolean, buf: Buffer, offset: number, length: number, position: number | null, callback: (...args: any) => void];
export declare const getWriteSyncArgs: (fd: number, a: unknown, b?: unknown, c?: unknown, d?: unknown) => [fd: number, buf: Buffer, offset: number, length: number, position: number | null];
export interface WriteArgs {
    buffer: Buffer;
    offset: number;
    length: number;
    position: number | null;
}
