import { SystemError } from './SystemError';
import type { StatError } from './types';
/** The own keys Node puts on every libuv error, in Node's key order. */
export interface ErrnoException extends Error {
    errno?: number;
    code: string;
    syscall: string;
    path?: string;
    dest?: string;
}
export declare const createError: (code: string, syscall?: string, path?: string, dest?: string, Constructor?: ErrorConstructor) => ErrnoException;
export declare const createStatError: (code: string, syscall?: string, path?: string, dest?: string) => StatError;
export interface WatchErrnoException extends ErrnoException {
    filename: string;
}
export declare const createWatchError: (code: string, filename: string) => WatchErrnoException;
export declare const createEisdirError: (syscall: string, path: string) => SystemError;
